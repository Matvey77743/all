program reyfuerufy;
var 
a,b,c, res : integer;
begin
  writeln ('введите первое число =');
  readln (a);
  writeln ('введите второе число =');
  readln (b);
  writeln ('введите третье число =');
  readln (c);
 res:=0;
 if a>0 then inc(res);
  if b>0 then inc(res);
   if c>0 then inc(res);
   writeln(res);
   end.
 